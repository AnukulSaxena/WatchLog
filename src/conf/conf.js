const conf = {
    tmdbToken: String(import.meta.env.VITE_APP_TMDB_TOKEN),
    expressUrl: String(import.meta.env.VITE_APP_EXPRESS_URL),
    openAiKey: String(import.meta.env.VITE_APP_OPENAI_KEY)
}

export default conf